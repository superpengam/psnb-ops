# TRACK=ENG

工程纪律，来自可组合小技能，不接管整个流程。

1. 需求不清：先问到每个分支有答案，再写代码。术语写进 `cases/<slug>/CONTEXT.md`。
2. 实现：先失败测试，再最小实现，再收口。不夹带无关重构。
3. 难 bug：复现 → 缩小 → 假设 → 插桩 → 修 → 回归。
4. 会话要断：`python scripts/handoff.py --target T --done "..." --next "..."`。
5. 审查两条轴分开：是否符合仓库规范；是否真做了约定的事。
